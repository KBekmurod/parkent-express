const sessionManager = require('../middleware/sessionManager');
const messages = require('../messages/uzbek.messages');

async function setupOrderCreationScene(bot) {
  return true;
}

module.exports = { setupOrderCreationScene };
